import React from "react";
import { Col } from "reactstrap";

const About = (props) => {
  return (
    <React.Fragment>
      <Col md="12">
        <h2>Landing</h2>

        <p>
          Welcome to the landing! From here you can search for a character to
          write a review on, look at all the reviews on the database, or look at
          the reviews you have posted.
        </p>
      </Col>
    </React.Fragment>
  );
};

export default About;
